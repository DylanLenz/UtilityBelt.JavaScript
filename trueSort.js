//General Purpose Array Sorting Algorithm.
var array = ;//Input
  for(var i = 0; i < array.length; i++){
    var rootComparitor = 0;
    var shiftingComparitor = 1;
  while(rootComparitor < array.length;){
  if(array[rootComparitor]>=array[shiftingComparitor]){
    array.push(numArray[shiftingComparitor]);
    array.slice(1,1);
  }else{
    array.push(numArray[rootComparitor]);
    array.slice(0,1);
  }
  }
  }
  console.log(array);
